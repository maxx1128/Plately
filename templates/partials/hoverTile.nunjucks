{% macro hoverTile(link, headline) %}
	
	<a href="{{ link }}">
		<div class="hover-tile-outer">
		  <div class="hover-tile-container">
		    <div class="hover-tile hover-tile-visible"></div>
		    <div class="hover-tile hover-tile-hidden">
		      <h4>{{ headline }}</h4>
		      {{ caller() }}
		    </div>
		  </div>
		</div>
	</a>

{% endmacro %}